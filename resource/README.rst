EVE resources
=============

Fonts
-----

Two files are contained for each ROM font containing a dump of the ROM data for font handles 16-34 as described in the datasheet.

romfontNN.evf
    Font metric block
romfontNN.raw
    Raw glyph data: zero-width glyphs are omitted.

This allows the fonts to be used with the Host virtual screen.
